# build output
dist/
.astro/

# dependencies
node_modules/

# secrets thật (KHÔNG commit — xem .env.example)
# Lưu ý: src/config/site.ts KHÔNG gitignore — chỉ chứa brand/SĐT/Zalo công khai
# (đã hiển thị trên mọi trang), cần có mặt lúc build static site trên CI/CD.
.env
.env.production
.env.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/
